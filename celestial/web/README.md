# Celestial — static website

Standards-based HTML5 + CSS3 + JS. Each page links a shared `styles.css` and `celestial-runtime.js` (interactions, sticky, particles, motion, perspective). Serve this folder with any static host — no build step (e.g. `npx serve .`).
